h2.visually-hidden Список тем
ul.themes
  each category in categories
    li.themes__item
      a.themes__item-link(class=(+id === category.id ? `themes__item-link--active` : `undefined`))(href=`/articles/category/${category.id}`)
        | #{category.name}
        sup #{category.count}
